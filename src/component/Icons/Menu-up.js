import React from 'react'
import MenuSideIcon from '../../assets/images/menu-side.svg'

const MenuSide = (props={}) => {
    return (
      <img src={MenuSideIcon} className={props.className}/>
    )
}

export default MenuSide
