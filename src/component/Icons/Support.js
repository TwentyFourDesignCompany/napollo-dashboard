import React from 'react'
import SupportIcon from '../../assets/images/headset.svg'

const Support = (props={}) => {
    return (
      <img src={SupportIcon} className={props.className}/>
    )
}

export default Support
