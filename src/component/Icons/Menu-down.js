import React from 'react'
import MenuDownIcon from '../../assets/images/menu-down.svg'

const MenuDown = (props={}) => {
    return (
      <img src={MenuDownIcon} className={props.className}/>
    )
}

export default MenuDown
